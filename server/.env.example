# SMTP (ustaw wg swojego dostawcy, np. OVH / Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=twoj_email@domena.pl
SMTP_PASS=HASLO_APLIKACYJNE_ALBO_SMTP
SMTP_SECURE=true            # true dla portu 465, false dla 587

# Adres docelowy (gdzie ma przyjść wiadomość)
MAIL_TO=gdzie_ma_przychodzic@domena.pl

# CORS — dozwolone originy (oddzielone przecinkiem)
CORS_ORIGIN=http://localhost:5173

# Express
PORT=4000
NODE_ENV=development
TRUST_PROXY=false           # ustaw true, jeśli za proxy/LoadBalancerem

# Rate limit
RATE_LIMIT_MAX=30           # ile zapytań na 10 minut z jednego IP
